name: "Code coverage file"
on: workflow_dispatch
jobs:
  "firstjob":
      runs-on: ubuntu-latest
      steps:
        - name: "print greeting"
          run: echo "welcome"
        - name: "multi input"
          run: |
            echo "welcome"
            echo "welcome 2"
        - name: "print bye bye"
          run: echo "Done Bye"
